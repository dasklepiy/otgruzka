
&НаКлиенте
Процедура ОчиститьЖурнал(Команда)
	
	Если Вопрос("Очистить журнал?", РежимДиалогаВопрос.ДаНет) = КодВозвратаДиалога.Нет Тогда
		
		Возврат;	
	КонецЕсли;
	
	ОчиститьЖурналСервер();
	Элементы.Список.Обновить();
КонецПроцедуры
&Насервере
Процедура ОчиститьЖурналСервер()
НаборЗаписей=РегистрыСведений.ЖурналИзмененийДокументов.СоздатьНаборЗаписей();
НаборЗаписей.Записать();
Константы.СчетчикЖурналаИзмененийДокументов.Установить(1);

КонецПроцедуры
